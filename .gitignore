logs/
.overnight_handoff.lock/
node_modules/
.next/
*.tsbuildinfo
data/
.env
.env.local
.env*.local
.vercel
.env*

# Local embedding model files, fetched on first use by src/lib/embedding.ts.
# ~25MB of ONNX weights: reproducible from the pinned model id, so not committed.
.model-cache/

# Working screenshots from the redesign; final ones live in docs/
.screenshots/

# College logos, fetched locally by `npm run logos:fetch`.
# Never committed: a logo is a trademark, and committing the files would
# publish them regardless of the SHOW_SCHOOL_LOGOS flag. See
# docs/school-logos.md.
public/school-logos/
